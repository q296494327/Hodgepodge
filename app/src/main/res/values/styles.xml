<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/colorTitle</item>
        <item name="colorControlNormal">@color/colorTitle</item>
    </style>

    <style name="quick_option_dialog" parent="@style/Base.Theme.AppCompat.Dialog.FixedSize">
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 动画 -->
        <item name="@android:windowEnterAnimation">@anim/dialog_enter</item>
        <item name="@android:windowExitAnimation">@anim/dialog_exit</item>
    </style>

    <!--popupwindow的动画-->
    <style name="style_popupwindow_audio">
        <item name="android:windowEnterAnimation">@anim/popupwindow_show</item>
        <item name="android:windowExitAnimation">@anim/popupwindow_hint</item>
    </style>

</resources>
